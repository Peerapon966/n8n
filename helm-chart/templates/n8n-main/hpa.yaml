{{- if .Values.services.n8nMain.enableAutoScaling }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "stock.fullname" . }}-n8n-main
  labels:
    {{- include "stock.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "stock.fullname" . }}-n8n-main
  minReplicas: {{ .Values.services.n8nMain.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.services.n8nMain.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.services.n8nMain.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.n8nMain.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.services.n8nMain.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.services.n8nMain.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
